import { categoryName, venueIcon } from '../../utils/storage.js';
import styles from './Detail.module.css';

function badge(venue) { return venue.halalStatus === 'certified' ? <span className="badge certified">✓ Halal Certified</span> : venue.halalStatus === 'muslim_friendly' ? <span className="badge friendly">✦ Muslim-Friendly</span> : <span className="badge unverified">! Unverified</span>; }
export default function Detail({ venue, onBack, onReport, onRoute, onMap }) {
  if (!venue) return null;
  return <section className={`${styles.root} screen active`}><header className="subheader"><button className="icon" onClick={onBack}>‹</button><button className="secondary small" onClick={onReport}>รายงาน</button></header><div className="detail"><div className="detail-hero">{venue.image ? <img src={venue.image} alt={`รูป ${venue.name}`} /> : <span>{venueIcon(venue)}</span>}</div><div>{badge(venue)}<h2>{venue.name || 'ไม่ระบุชื่อสถานที่'}</h2><p><b>{categoryName(venue)}</b>{venue.address ? ` · ${venue.address}` : ''}</p><p>{venue.verificationNote || 'ข้อมูลจาก OpenStreetMap และยังรอการตรวจสอบ'}</p><div className="actions">{venue.latitude && venue.longitude && <button className="secondary small" onClick={onMap}>ดูบนแผนที่</button>}<button className="primary" onClick={onRoute}>นำทางในแอพ</button></div><section className="detail-block"><h3>ข้อมูลสถานที่</h3>{venue.cuisine && <div className="detail-row"><span>ประเภท</span><span>{venue.cuisine}</span></div>}{venue.phone && <div className="detail-row"><span>โทรศัพท์</span><span>{venue.phone}</span></div>}{venue.website && <div className="detail-row"><span>เว็บไซต์</span><span>{venue.website}</span></div>}<div className="detail-row"><span>แหล่งข้อมูล</span><span>OpenStreetMap</span></div></section><div className="warning">สถานะฮาลาลของรายการนี้ยังไม่ได้รับรองจากผู้ดูแลระบบ ห้ามใช้รายการนี้แทนใบรับรองฮาลาล</div></div></div></section>;
}